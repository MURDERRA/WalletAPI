# Wallet API

REST API для управления кошельками с поддержкой операций пополнения и снятия средств.

## 🚀 Ключевые особенности

- **Высокая производительность**: Обработка до 1000 RPS на один кошелек
- **Конкурентность**: Безопасная работа с параллельными запросами благодаря транзакциям PostgreSQL с `FOR UPDATE`
- **Docker-ready**: Полная контейнеризация приложения и базы данных
- **Swagger документация**: Интерактивная документация API
- **Логирование**: Детальное логирование всех операций

## 🏗️ Структура проекта

```text
WalletAPI/
├── cmd/
│   └── main.go                 # Точка входа приложения
├── internal/
│   ├── config/
│   │   └── config.go           # Конфигурация приложения
│   ├── model/
│   │   └── model.go            # Модели данных
│   ├── repository/
│   │   └── repository.go       # Слой работы с БД
│   └── service/
│       └── service.go          # HTTP handlers
├── tests/
│   └── wallet_test.go          # Интеграционные тесты
├── migrations/                 # Инициализация БД и миграции
│   ├── 01_create_database.sql              
│   └── 02_create_tables.sql
├── docs/                       # Swagger документация
├── logs/                       # Логи приложения
├── databases/                  # PostgreSQL data
├── docker-compose.yml
├── Dockerfile
├── .dockerignore
├── config.env                  # Конфигурация
├── go.mod
├── go.sum
└── README.md
```

## 🚀 Быстрый старт

### 1. Клонируйте репозиторий

```bash
git clone https://github.com/MURDERRA/WalletAPI
cd WalletAPI
```

### 2. Создайте конфигурационный файл

```bash
cp config.env.example config.env
# Отредактируйте config.env, установите надёжный пароль
```

### 3. Запустите приложение

```bash
docker compose up --build -d
```

### 4. Проверьте статус

```bash
docker compose ps
```

Возможно потребуется ручное создание таблицы, sql скрипт для этого в папке migrations с названием 02_create_tables.sql

### 5. Откройте Swagger UI

Перейдите по адресу: [http://localhost:8080/swagger/index.html](http://localhost:8080/swagger/index.html)

### Обработка ошибок

Все ошибки возвращаются в стандартном формате:

```json
{
  "success": false,
  "error": "Описание ошибки"
}
```

**Коды ответов:**

- `200` - Успешная операция
- `400` - Неверный запрос (некорректные данные)
- `404` - Кошелек не найден
- `500` - Внутренняя ошибка сервера

## 🧪 Тестирование

### Запуск интеграционных тестов

```bash
# Все тесты
go test ./tests/... -v

# Быстрые тесты (без стресс-теста)
go test ./tests/... -v -short

# Конкретный тест
go test ./tests/... -v -run TestAPI_Concurrent_SingleWallet_1000Requests

# Стресс-тест (1000 RPS, 10 секунд)
go test ./tests/... -v -run TestAPI_Stress_1000RPS -timeout 30m
```

### Описание тестов

1. **TestAPI_CreateWallet** - Создание кошелька
2. **TestAPI_DepositAndWithdraw** - Базовые операции пополнения/снятия
3. **TestAPI_Concurrent_SingleWallet_1000Requests** - 1000 параллельных запросов на один кошелек
4. **TestAPI_Concurrent_MultipleWallets** - Параллельная работа с несколькими кошельками
5. **TestAPI_Stress_1000RPS** - Стресс-тест: 1000 запросов в секунду в течение 10 секунд

## 🔧 Разработка

### Локальный запуск (без Docker)

1. Запустите PostgreSQL:

```bash
docker compose up postgres -d
```

2. Обновите `config.env`:

```env
POSTGRES_URL=postgres://postgres:your_password@localhost:5432/walletsdb
```

3. Установите зависимости:

```bash
go mod download
```

4. Сгенерируйте Swagger документацию:

```bash
go install github.com/swaggo/swag/cmd/swag@latest
swag init -g main.go --parseInternal
```

5. Запустите приложение:

```bash
go run main.go
```

### Обновление Swagger документации

После изменения комментариев к API:

```bash
swag init -g main.go --parseInternal
```

### Просмотр логов

```bash
# Логи API
docker compose logs -f wallet-api

# Логи PostgreSQL
docker compose logs -f postgres

# Все логи
docker compose logs -f

# Логи на диске
tail -f logs/WalletAPI .log
```

## 🗄️ База данных

### Схема таблицы `wallets`

```sql
CREATE TABLE IF NOT EXISTS wallets (
    uuid UUID PRIMARY KEY,
    balance DECIMAL NOT NULL DEFAULT 0
);

CREATE INDEX idx_wallets_uuid ON wallets(uuid);
```

### Миграции

Миграции выполняются автоматически при первом запуске контейнера PostgreSQL из файлов в папке `migrations/`.

Для применения новых миграций вручную:

```bash
docker compose exec postgres psql -U postgres -d wallets_db -f /docker-entrypoint-initdb.d/new_migration.sql
```

## 📊 Производительность

### Настройки PostgreSQL для высокой нагрузки

В `docker-compose.yml` оптимизированы параметры:

- `max_connections=200` - максимум соединений
- `shared_buffers=256MB` - буфер памяти
- `effective_cache_size=1GB` - размер кеша

### Настройки Connection Pool

В коде настроен пул соединений:

- `MaxConns: 200` - максимум соединений
- `MinConns: 20` - минимум активных соединений
